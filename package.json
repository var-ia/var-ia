{
  "name": "varia",
  "version": "0.2.1",
  "type": "module",
  "private": false,
  "description": "A deterministic claim-provenance engine for Wikipedia page histories. Evidence, not truth.",
  "repository": "github:nextconsensus/var-ia",
  "homepage": "https://nextconsensus.com",
  "bugs": "https://github.com/nextconsensus/var-ia/issues",
  "keywords": [
    "wikipedia",
    "evidence-graph",
    "claim-provenance",
    "diff-analysis",
    "edit-history",
    "deterministic",
    "observation-engine"
  ],
  "engines": {
    "bun": ">=1.2.0",
    "node": ">=20.0.0"
  },
  "license": "AGPL-3.0",
  "workspaces": [
    "packages/ingestion",
    "packages/evidence-graph",
    "packages/analyzers",
    "packages/interpreter",
    "packages/cli",
    "packages/persistence",
    "packages/eval"
  ],
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint packages/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf packages/*/dist"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^20.12.0",
    "eslint": "^10.3.0",
    "typescript": "^5.4.0",
    "typescript-eslint": "^8.59.3",
    "vitest": "^1.6.0"
  }
}
